[[ render 1 ]]
>  Select a shell theme.
   Cross-platform prompt using the Starship binary.
   * starship
   o powerlevel10k
   o default
   Use Up/Down to choose, Enter to confirm.

[[ render 2 ]]
>  Select a shell theme.
   Keep the default shell prompt styling.
   o starship
   o powerlevel10k
   * default
   Use Up/Down to choose, Enter to confirm.

=  Select a shell theme.
   default

[[ value ]]
default
